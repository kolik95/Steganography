<Pages:BasePage x:TypeArguments="VMs:LSBEncViewModel" x:Class="WindowsUI.LSBEncPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WindowsUI"
      xmlns:Pages="clr-namespace:WindowsUI.Pages"
      xmlns:VMs="clr-namespace:WindowsUI.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LSBEncPage">

    <Border Background="{Binding Background}" 
            Padding="30">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <StackPanel Orientation="Horizontal"
                            Grid.Row="0">
                    <Label 
                        Content="Formát textu"
                        FontSize="15"
                        Margin="0 0 10 0"/>
                    <ComboBox
                        Width="Auto"
                        FontSize="15"
                        IsEditable="False" 
                        SelectedIndex="0"
                        SelectionChanged="Selector_OnSelectionChanged">
                        <ComboBoxItem>ASCII</ComboBoxItem>
                        <ComboBoxItem>UTF-8</ComboBoxItem>
                    </ComboBox>
                </StackPanel>
                <TextBox
                    Margin="0 20"
                    Text="Text k zašifrování"
                    x:Name="EncText"
                    FontSize="20"
                    TextWrapping="Wrap"
                    AcceptsReturn="True"
                    VerticalScrollBarVisibility="Auto"
                    Grid.Row="1"/>
            </Grid>
            <Grid Grid.Column="1" Margin="25 0 0 0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Border BorderThickness="0.5"
                        Margin="0 0 0 10"
                        Background="Transparent"
                        x:Name="ImageBorder"
                        Grid.Row="0">
                    <Border.Triggers>
                        <EventTrigger SourceName="ImageGrid"
                                      RoutedEvent="Grid.DragEnter">
                            <BeginStoryboard>
                                <Storyboard TargetName="ImageBorder" TargetProperty="Background.Color">
                                    <ColorAnimation From="Transparent"
                                                    To="LightGray"                                       
                                                    Duration="0:0:0.15"
                                                    IsAdditive="False"
                                                    IsCumulative="False"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                        <EventTrigger SourceName="ImageGrid"
                                      RoutedEvent="Grid.DragLeave">
                            <BeginStoryboard>
                                <Storyboard TargetName="ImageBorder" TargetProperty="Background.Color">
                                    <ColorAnimation From="LightGray"
                                                    To="Transparent"                                       
                                                    Duration="0:0:0.15"
                                                    IsAdditive="False"
                                                    IsCumulative="False"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </Border.Triggers>
                    <Grid AllowDrop="True" 
                          Drop="Grid_Drop"
                          x:Name="ImageGrid"
                          Background="Transparent">
                        <TextBlock Text="Vložte obrázek" 
                                   TextAlignment="Center" 
                                   VerticalAlignment="Center" 
                                   Background="Transparent" 
                                   Foreground="Gray"/>
                        <Image Source="{Binding DragImage}"
                               x:Name="Image"/>
                    </Grid>
                    <Border.BorderBrush>
                        <DrawingBrush Viewport="0,0,40,40" ViewportUnits="Absolute" TileMode="Tile">
                            <DrawingBrush.Drawing>
                                <DrawingGroup>
                                    <GeometryDrawing Brush="Black">
                                        <GeometryDrawing.Geometry>
                                            <GeometryGroup>
                                                <RectangleGeometry Rect="0,0,50,50" />
                                                <RectangleGeometry Rect="50,50,50,50" />
                                            </GeometryGroup>
                                        </GeometryDrawing.Geometry>
                                    </GeometryDrawing>
                                </DrawingGroup>
                            </DrawingBrush.Drawing>
                        </DrawingBrush>
                    </Border.BorderBrush>
                </Border>
                <Grid VerticalAlignment="Stretch"
                      HorizontalAlignment="Stretch"
                      Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <DockPanel Grid.Row="0">
                        <Button Width="20"
                                Height="20"
                                Command="{Binding GetImageCommand}">
                            <Image Source="../Resources/Images/OpenFile.png"/>
                        </Button>
                        <TextBox 
                                 Margin="20 0 0 0"
                                 Text="{Binding ImagePath, Mode=TwoWay}"/>
                    </DockPanel>
                    <Button Height="20"
                            Width="80"
                            HorizontalAlignment="Left"
                            Content="Uložit"
                            Command="{Binding SaveCommand}"
                            CommandParameter="{Binding ElementName=EncText, Path=Text}"
                            Grid.Row="1"
                            Margin="0 5"/>
                </Grid>
            </Grid>
        </Grid>
    </Border>
</Pages:BasePage>